//指定版本
//注意proto3与proto2的写法有些不同
syntax = "proto3";
 
//包名，通过protoc生成时go文件时
option go_package="/msg";

message ReqLogin {
    string account = 1;
}

message RespLogin {
    string account = 1;
    uint32 result = 2;
}